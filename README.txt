SETUP:
  -Download Node.js (https://nodejs.org/en/)
  -Open up terminal (macs) or command line (windows) and change directories to the root directory of 'project_1'
  -run 'npm install' to install all the appropriate package dependencies
  -run 'node server.js' to run the server
  -open up a web browser and go to https://localhost:8080
